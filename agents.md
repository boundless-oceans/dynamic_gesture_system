# 项目状态

## 概述
DSTE-Net 动态手势识别桌面应用，文化馆非遗展示系统。PySide6 + OpenCV + PyTorch。
模型：ResNet-50 + DSTE（LSTE 坐标注意力+ECA 动态卷积 / GSTE 帧间差分+空间注意力），
8 帧 RGB，**IPN-Hand 13 类**（B0A…G11）。
路径：本仓库根目录；素材来源：文化馆提供的项目资料（原始资料目录不在版本库中）。

## 功能清单

| 编号 | 功能 | 状态 |
|------|------|:--:|
| F0 | 入口（启动画面） | 已完成 |
| F1 | 摄像头预览（自动优先外接，CLAHE 仅预览） | 已完成 |
| F2 | 推理线程（显示/触发双结果） | 已完成 |
| F2a | 帧缓冲队列 | 已完成 |
| F2b | 模型加载与推理（strict 加载权重） | 已完成 |
| F3 | 置信度显示（含"已执行"提示） | 已完成 |
| F4 | 主窗口（6 页 + 路由 + 去抖/冷却/锁存） | 已完成 |
| F5 | 防误触发（冷却/去抖/锁存/松手复位/最大锁存） | 已完成 |
| F6 | 标签映射（13 类中文名 + 控制动作） | 已完成 |
| F7 | 手势映射（13 类 → 控制动作，按页面路由） | 已完成 |
| F8 | 首页（6 项非遗卡片轮播 + 印章 + 粒子背景） | 已完成 |
| F9 | 详情页主视图（作品轮播 + 项目信息卡 + 三按钮） | 已完成 |
| F9b | 详情页·非遗详情（可滚动介绍 + 视频 + 3 图 + 二维码） | 已完成 |
| F10 | 3D 查看（three.js r147 + GLTFLoader 加载 GLB） | 已完成 |
| F11 | 设置页（手势对照表） | 已完成 |
| F12 | 传承人风采（数据驱动，5 位，视频/照片切换） | 已完成 |
| F13 | 非遗地图（高德瓦片、地名常显、手势平移缩放） | 已完成 |
| F14 | 视频手势快进/快退（左/右抛出 30s） | 已完成 |
| P3 | 日志系统 | 已完成 |
| — | 内容配置化（projects.json / inheritors.json） | 已完成 |
| — | 素材生成脚本（图片/模型/轮播图） | 已完成 |
| — | 毛玻璃 UI / 页面切换动画 | 已完成 |

## 当前进度
- 模型权重：IPN-Hand 13 类，`weights/TSQ_ipnhand_RGB_resnet50_shift0.50_blockres_avg_segment8_e50.pth`
  （服务器训练产出，best_prec1 88.97%，**未入库**）
- 素材：图片/3D 模型/视频均已接入（视频已转码为 720p，共约 124MB）
- 识别实测：大部分手势可用；偏弱类别（放大等）未绑定动作

### 待办
- **打包 exe**：PyInstaller 配置未做（体积约 1.3GB，需含 QtMultimedia+FFmpeg 后端 DLL）
- **设置页加"图片来源"**：CC BY-SA 素材（包公祠实景照）要求署名，见 `assets/images/CREDITS.md`
- **权重瘦身**：当前权重含 optimizer（227MB），可提取 state_dict 到 114MB（fp16 约 57MB）
- **素材缺口**：火笔画、吴山铁字无二维码；包公仅有连环画+1 张实景照
- **文档现状**：现行说明只有本文件与 `README.md`。早期的 `DESIGN.md` 描述的是
  EgoGesture 83 类的旧方案（与现状全面不符），已删除（`git log -- DESIGN.md` 可查）；
  `docs/UI_DESIGN.md` 保留但只记布局比例，过时处已在文内标注

## 页面索引
| 索引 | 页面 |
|:--:|------|
| 0 | 传承人风采 |
| 1 | 首页 |
| 2 | 详情页（子视图 0=主视图，1=非遗详情）|
| 3 | 3D 查看 |
| 4 | 设置页 |
| 5 | 非遗地图 |

## 关键约定（改动前务必了解）

1. **n_segment = 8 由权重固定**（`CoordAtt.fc1` 输入维度即 8），不可随意更改；
   想换帧数必须重训。
2. **摄像头悬浮窗固定占用右上角 (940,20,320,240)**：各页右侧内容需下移让位；
   若仍重叠，**摄像头在最上层**（换页时 `raise_()` 保证）。
3. **不入库的目录**（`.gitignore`）：`weights/`、`assets/videos/`、`assets/models/`。
   克隆后需自行放置权重与视频，或跑 `tools/` 脚本生成模型。
4. **视频用 `QMediaPlayer` 播 mp4**：Qt 的 Chromium **不支持 H.264**，不能走网页播放器。
5. **3D 用 three.js r147**（UMD）+ 配套 `GLTFLoader.js`：r150 之后官方不再提供 UMD 版 loader。
6. **手势参数集中在 `src/config.py`**（阈值/平滑/去抖/冷却/锁存/快进步长/摄像头选择）。
7. **素材脚本在 `tools/`**：`prepare_assets.py`（图片，含裁剪与轮播图）、
   `fbx_to_glb.py`（FBX→GLB，烘焙骨骼+归一化）、`render_slides.py`（Blender 渲染轮播图）。
   源素材来自文化馆项目资料（目录不在版本库中，路径见脚本顶部常量），脚本可重复执行（会先清理旧产物）。
8. **训练代码**：在服务器上运行，训练仓库单独维护，不在本仓库内；已做的改进见 README 第五节。
