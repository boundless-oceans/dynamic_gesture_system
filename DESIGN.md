# 非遗动态手势展示系统 - 设计文档

## 一、页面设计（4 页）

| 页面 | 文件 | 内容 | 说明 |
|------|------|------|------|
| 首页 | `pages/index.html` | 非遗项目横向轮播，当前选中项高亮 | 左右滑动切换项目 |
| 详情页 | `pages/detail.html` | 选中项目的图文/视频详细介绍 | 上下滚动浏览内容 |
| 3D 查看 | `pages/viewer.html` | Three.js 3D 模型旋转交互 | 手势控制旋转、缩放 |
| 设置页 | `pages/settings.html` | 手势列表对照表、操作说明 | 静态展示 |

摄像头画面悬浮于右上角，全局可见。

```
┌──────────────────────────────────────────────┐
│                  非遗展示系统                   │
│                  ┌────────┐                   │
│                  │摄像头小窗│  ← 悬浮右上角     │
│                  └────────┘                   │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌─────┐│
│  │  首页   │  │ 详情页  │  │3D查看  │  │设置 ││
│  │ 轮播展示 │  │图文介绍 │  │模型交互 │  │手势  ││
│  └────────┘  └────────┘  └────────┘  └─────┘│
└──────────────────────────────────────────────┘
```

## 二、手势映射（9 种动态手势 → 页面操作）

| 动态手势 | 首页操作 | 详情页操作 | 3D页操作 |
|------|------|------|------|
| swipe_left | 上一项 | — | — |
| swipe_right | 下一项 | — | — |
| swipe_up | — | 向上滚动 | — |
| swipe_down | — | 向下滚动 | — |
| click | 进入详情 | — | — |
| palm | 回到首页（从其他页） | 回到首页 | 回到首页 |
| zoom_in | — | — | 放大模型 |
| zoom_out | — | — | 缩小模型 |
| circle | — | — | 旋转模型 |

> EgoGesture 83 类手势中选取 9 种作为控制手势，其余手势不绑定操作。
> 63 类手势为备选，后续可扩展。

## 三、页面流转

```
                    ┌─────────┐
          click     │  首页    │     palm
        ──────────▶│ 轮播浏览  │◀──────────
       │           └────┬─────┘           │
       ▼                │                 │
  ┌─────────┐     palm  │    ┌─────────┐  │
  │ 详情页   │◀──────────┘    │3D 查看  │  │
  │ 上下滚动 │               │旋转缩放  │──┘
  └─────────┘               └─────────┘
```

- 首页 click 某个项目 → 进入详情页
- 首页 palm（手张开）→ 进入 3D 查看页
- 任何子页 palm → 回到首页
- 鼠标点击按钮作为兜底操作

## 四、功能需求分解

### P0（核心，必须最先完成）

| 编号 | 功能 | 描述 |
|:--:|------|------|
| F2b | 模型加载 | 加载 DSTE-Net 结构 + 权重，提供 predict() 接口 |
| F2a | 帧缓冲 | 线程安全环形队列，保持最近 N 帧，支持取出 8 帧 |
| F1 | 摄像头预览 | OpenCV 采集 → 画面悬浮窗 |
| F2 | 推理线程 | 从帧缓冲取 8 帧 → 预处理 → 推理 → 输出手势类别 |
| F7 | 手势映射 | 83 类 DSTE-Net 输出 → 9 种控制手势（映射表） |
| F4 | 主窗口 | 4 页框架 + 页面切换 + 摄像头悬浮窗 + 信号槽串联 |
| F0 | 入口 | main.py，启动 QApplication |

### P1（页面内容，后续完善）

| 编号 | 功能 | 描述 |
|:--:|------|------|
| F8 | 首页 | 非遗项目轮播展示，左右滑动切换 |
| F9 | 详情页 | 图文介绍，上下滚动 |
| F10 | 3D 查看 | Three.js 3D 模型交互（旋转、缩放） |
| F5 | 去抖 | 连续 N 次识别结果一致才触发操作 |
| F3 | 置信度显示 | 摄像头小窗旁显示当前手势 + 置信度 |

### P2（锦上添花）

| 编号 | 功能 | 描述 |
|:--:|------|------|
| F11 | 设置页 | 手势列表对照表 |
| F6 | 标签映射 | 83 类英文 → 中文（gestures.json） |

## 五、实现顺序

```
F2b → F2a → F1 → F2 → F7 → F4 → F0   [核心链路]
                                    ↓
                         F8 → F9 → F10  [页面内容]
                                    ↓
                              F5, F3, F11, F6
```
